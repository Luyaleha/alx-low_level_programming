#include <stdio.h>
/**
*main - writes 00Decri
*Decription: Writes all unique combinations*Return: zero for success
*Return: zero for success
*/
int main(void)
{
char letter;
for (letter = 'a'; letter <= 'z'; letter++)
putchar(letter);
putchar('\n');
return (0);
}
